# n1 — purser scaffolded

package.json (name purser, Apache-2.0, no inflow dependency), tsconfig with strict plus
exactOptionalPropertyTypes and noUncheckedIndexedAccess, vitest config, Apache-2.0 LICENSE
fetched from apache.org, README.

Apache-2.0 rather than MIT to match @x402/core and @x402/evm, which carry an explicit patent
grant. The vault's earlier licensing note set MIT as the default and Apache where a patent grant
matters for protocol tooling. This is that case.
