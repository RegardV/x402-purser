# n2 - the falsified claims corrected

`README.md`: the key custody section now separates what the split genuinely rules out (native
currency transfers, approve, arbitrary contract calls) from what it does not, and carries an
explicit "as of 0.1.0" note that the signer has no cumulative limit. Adds a section naming the hot
balance as the only bound that does not depend on Purser being correct, linking the threat model.
The does-not-claim list gained the drain vector and the absence of any detection.

`SECURITY.md`: the open finding is listed with its date, the proof of concept result, and the fix,
so anyone evaluating the package sees it before deploying rather than after.

The 2026-08-12 spec and plan keep their original text and gain a superseded banner. Silently
editing them would erase the record of what was believed at the time, which is the part worth
keeping.

Nothing in src or test changed. 153 tests still pass.
